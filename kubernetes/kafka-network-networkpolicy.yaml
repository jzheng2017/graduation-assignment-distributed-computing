apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: kafka-network
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/kafka-network: "true"
  podSelector:
    matchLabels:
      io.kompose.network/kafka-network: "true"
